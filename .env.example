TEST_BASE_URL=http://localhost:5000/
TEST_EMAIL=your_email@example.com
TEST_PASSWORD=your_password
HEADLESS=true
TIMEOUT=10

# Login field/button XPaths live in xpaths/login_xpaths.py

# Candidate add/create: edit `CANDIDATE_*_BUTTON_TESTID` in xpaths/candidate_xpaths.py
# to match the app’s data-testid (see `ADD_CANDIDATE_BUTTON_XPATH` there).

# Bulk candidate creation loop size
CANDIDATE_COUNT=100
